name: CFR Auto Update Every 5 Minutes

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes
  workflow_dispatch:

jobs:
  update-cfr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up Git user
        run: |
          git config user.name "cfr-auto-updater"
          git config user.email "cfr-auto-updater@example.com"

      - name: Run download script
        run: |
          chmod +x download_cfr.sh
          ./download_cfr.sh

      - name: Check for changes
        id: check_changes
        run: |
          if git diff --quiet cfr.tsv; then
            echo "No changes in cfr.tsv"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "Changes detected in cfr.tsv"
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push changes
        if: steps.check_changes.outputs.changed == 'true'
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git add app/correction_response/cfr.tsv
          git commit -m "Auto-update cfr.tsv [every 5 minutes]"
          git remote set-url origin https://x-access-token:${PAT_TOKEN}@github.com/${{ github.repository }}
          git push origin HEAD
