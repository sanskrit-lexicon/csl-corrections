# Project Objective: Sanskrit Lexicon (CDSL) correction-workflow follow-ups

Apply markup corrections to `csl-orig`, keep the `csl-corrections` audit trail in sync, close the matching GitHub issues per the org taxonomy, and keep the runbooks/docs current. This file continues the 2026-05-29 four-task handoff (Tasks 1–4 all complete — see ✅ below). **New chat? The consolidated cross-stream start-here is [`csl-observatory/docs/SESSION_HANDOFF.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/SESSION_HANDOFF.md) (2026-05-30 section), and open maintainer items are in [`DECISIONS_NEEDED.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/DECISIONS_NEEDED.md).**

## ➡️ Next Steps (Queue)

- [x] **(Done 2026-05-30) Taxonomy core applied to all 6 milestone-less repos.** BOR, BUR, INM, KRM, BOP, MW72 — all 4 standard milestones created per repo, all issues classified with exactly one type label + one severity label + correct milestone + org project assignment (projects 1–4). Phase 7 verification: ALL OK across all 6 repos (31 issues total). See ✅ Completed for details.
- [x] **(Done 2026-05-30) hw.py BOM-hardening filed as [csl-pywork#50](https://github.com/sanskrit-lexicon/csl-pywork/issues/50).** `bug`/`minor`/Developer Experience. Proposes changing `encoding='utf-8'` → `encoding='utf-8-sig'` in `init_entries` and `init_entries_kosha` so a stray BOM can't masquerade as "init_entries Error 2". Routed to maintainers — not patched silently.
- [ ] **(Server-side, NOT local) Public-artefact refresh.** Handled by `csl-pywork/v02/redo_xampp_selective.sh` on the Cologne server via cron. csl-orig fixes are already on `origin/master`, so the next server run propagates to Stardict/JSON/homepage automatically. A Cologne admin (Dhaval/Jim) can trigger it manually for a faster refresh. NOT runnable on this Windows box (hardcoded server path, python2, 5 absent sibling repos, cross-org push creds). See memory `project_artefact_refresh_serverside`.
- [ ] **(Optional) Remaining full-runbook gaps on the 6 documented repos.** (a) `.github/ISSUE_TEMPLATE/*.yml` + PR template were NOT added — `csl-observatory/templates/` holds only the 3 community files, no issue templates. (b) `LICENSE` files are pointer-style CC BY-SA 4.0 (SPDX id + canonical links), not the full legalcode — add full text if GitHub license auto-detection is wanted. (c) Verify Source-bibliography fields filled from cff + known facts (publishers/places/co-authors), esp. BUR (Leupol / Maisonneuve) and BOP (1847 edition). (d) KRM `LICENSE` is GPL-3.0 vs its cff CC-BY-SA-4.0 — flagged in `KRM/CLAUDE.md`; needs a maintainer decision.
- [~] **Engineering roadmap — quick wins Q1–Q6 IMPLEMENTED 2026-05-30** (autonomous batch; see ✅ Completed). Remaining in [ROADMAP.md](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/ROADMAP.md):
  - **Q2 XML-parse extension** — csl-orig guard now checks BOM + UTF-8 + `<L>/<LEND>` balance (`cbcf6be`); only the full `make_xml` parse check (needs csl-pywork) remains on [csl-pywork#51](https://github.com/sanskrit-lexicon/csl-pywork/issues/51) (kept open).
  - **M1** modernise `redo_xampp_selective.sh` ([csl-pywork#53](https://github.com/sanskrit-lexicon/csl-pywork/issues/53)) — needs server/python2 testing; NOT done unattended.
  - **M4** automate observatory refresh ([csl-observatory#12](https://github.com/sanskrit-lexicon/csl-observatory/issues/12)) — needs a token + touches the live dashboard workflow; left for supervised run.
  - **Q7 taxonomy — ✅ FULLY DONE 2026-05-31** (see ✅ Completed). **24 dictionary repos** (786 issues, dict taxonomy) **and 4 tooling repos** csl-apidev/cologne-stardict/GreekInSanskrit/ArabicInSanskrit (153 issues, tooling taxonomy) all verified all-clean. The finer per-issue re-audit showed the "22-repo" 2026-05-30 figure understated it: several repos previously marked done (AP/AP90/GRA/MWS/PWG/PWK/MD/FRI/Wil-YAT) had accumulated unlabeled issues, and PWG hid 16 pre-existing multi-type issues beyond the REST 100-issue page. **M3 full-runbook README/CLAUDE — ✅ DONE 2026-05-31** for the 13 stub repos (WIL + ACC, BEN, BHS, CAE, CCS, LRV, MCI, SCH, SKD, STC, VCP, VEI); 2 deferred (ApteES reverse-direction markup → B5, SHS authorship → B4). The org-wide `docs-pass` branches are a *separate* microanalysis effort (DICT_PROFILE.md), not M3. COLOGNE/CORRECTIONS/temp_corrections_* stay skipped (legacy/meta).
  - **M7** KRM GPL-vs-cff licensing — a maintainer *decision*, not a silent change.
  - Strategic S1–S5 (server→cloud migration, DTB linking, structured data, unified CI, papers programme). Data-quality/dict-text items stay in the roadmap doc per the comment-noise norm.

## 🚧 Current Work-In-Progress (WIP)

- [ ] (none active) — all four handoff tasks complete; only the optional items above remain.

## 🧠 Dev Notes & Hypotheses

- **2026-05-29 — the "<LEND> init_entries Error 2" was a misdiagnosis, now RESOLVED.** Not a pre-existing structural defect: it was a UTF-8 BOM that the markup commits accidentally added (ap90/mw/ap/bur/inm/krm). `hw.py` opens with plain `encoding='utf-8'` (no BOM strip), so a BOM'd line 1 (`﻿<L>…`) fails `startswith('<L>')`, the first `<L>` is skipped, and the next `<LEND>` raises Error 2. Commit `922602c` removed the BOMs; git archaeology confirms pre-markup state was clean and all 10 sources now parse balanced. **No csl-orig correction needed.** (Memory `feedback_csl_orig_workflow` updated.)
- **Maintainer comment-noise signal.** On MWS#194, funderburkjim & drdhaval2785 questioned the value of Claude co-authored commits/comments. Keep dictionary-repo issue comments minimal — edit existing comments in place over posting new. NB: `csl-observatory` (gasyoun's own infra repo) DOES use `Co-Authored-By` trailers by convention; the pushback was specifically about dictionary-repo issues. See memory `project_maintainer_comment_noise`.
- **Audit-trail gap (by design).** The BOM-removal commit `922602c` is intentionally NOT in `batch_20260529` (the prior session excluded encoding-only changes). It is the one csl-orig edit lacking an audit-trail change file, should strict completeness ever be wanted.
- **EOL note.** `csl-observatory` has `core.autocrlf=true` and no `.gitattributes`; runbook blobs are stored LF. When editing on Windows, write LF and verify `git diff` shows only intended lines (the "LF will be replaced by CRLF" warning is normal).

## ✅ Completed (Recent only)

- **2026-05-31 M3 full-runbook docs (README + CLAUDE) — 13 stub repos.** Generated and pushed full README.md + CLAUDE.md to **WIL** (hand-built template) + **ACC, BEN, BHS, CAE, CCS, LRV, MCI, SCH, SKD, STC, VCP, VEI** (data-driven from a curated bibliography + live issue data), matching the BOR-group gold standard: intro + canonical-source link, Contents/Architecture (tree-derived), Timeline, Projects & Milestones table + Mermaid pie, Issue Typology (Open/Solved tables + Mermaid pie by type), Labels, Contributors, Source bibliography, Encoding, pipeline-DAG flowchart, and a CLAUDE data-format table + **real first-entry annotated example** per dict. All 42 Mermaid blocks validated via `gh api markdown`. Pushed to each repo's default branch (master; LRV=main) with plain `docs:` commits, **no Claude trailer** (dict-repo convention). LRV kept its existing detailed CLAUDE (pipeline scripts) — README only. **Scope discovery:** the audit's "missing README" for PWG/PWK was a false alarm (they have lowercase `readme.md`, already full); repos with large READMEs (AMAR/AP/FRI/KNA/KOW/PUI/Wil-YAT/MWS/GRA/AP90/MD) were already M3-done. **Deferred (flagged in DECISIONS_NEEDED B4/B5):** ApteES (English→Sanskrit, non-standard `{@…@}`/`<s>` markup needs hand-crafted data-format) and SHS (author conflict: inventory Mukerji vs repo-CLAUDE Tarkavāchaspati). Scripts: `GitHub/_m3_*.py`.

- **2026-05-31 Q7 tooling-repo taxonomy rollout — 4 repos, all-clean.** Applied the **tooling** taxonomy (17 type labels across 5 color-groups + 4 severities trivial/minor/major/critical + 5 milestones API Stability/User Experience/Data Quality/Developer Experience/Community) to **csl-apidev, cologne-stardict, GreekInSanskrit, ArabicInSanskrit** (153 issues). Open issues → org project **#9 Tooling Roadmap**; closed issues labelled+milestoned but NOT boarded (matches the deployed convention — verified csl-pywork closed issues are off the board). GraphQL Phase-7 verify: **ALL CLEAN**. Hand-classified: ArabicInSanskrit's 15 "provide Arabic text" tasks → `enhancement`/`question` (mirroring GreekInSanskrit's existing `enhancement` convention for the analogous Greek tasks — consistency across the two sister repos); csl-apidev #8/#12 → `docs-api`, #25 multi→`bug`; cologne-stardict #32→`enhancement`, #48→`feature`. Rest auto-derived from existing type labels (they had types but no milestones — none of the 4 had any milestones). Ground-truth check first confirmed the **17-type runbook** (not the 9-type CLAUDE.md summary) is what the 28 processed tooling repos actually use. Scripts: `GitHub/_tool_*.py`. **A2 now fully closed.**

- **2026-05-31 Q7 dictionary-issue taxonomy rollout — 24 repos, all-clean.** Applied the dict taxonomy (exactly 1 type + 1 severity + 1 milestone + matching org project 1–4, MWS→5–8) to every issue across **ACC, AP, AP90, ApteES, BEN, BHS, CAE, CCS, FRI, GRA, LRV, MCI, MD, MWS, PWG, PWK, SCH, SHS, SKD, STC, VCP, VEI, WIL, Wil-YAT** (786 issues). **238 issues newly labelled; 400 project memberships added** (incl. backfill of older milestoned-but-unassigned issues). GraphQL Phase-7 verification: **ALL CLEAN** (0 noType / 0 multiType / 0 noSev / 0 noMS / 0 noProj per repo). Method: pure GitHub-API issue triage (no commits, no repo-file changes, no issue comments → comment-noise-norm safe). **Pull requests excluded** (dependabot bumps, docs-pass/Pages-fix PRs — they are not dictionary issues). Caught + fixed two audit blind spots: (a) REST `?per_page=100` hides issues beyond page 1 → PWG's 16 low-numbered pre-existing multi-type issues were only found via paginated GraphQL; (b) AP90#10 + VCP#16 unlabeled stragglers. Scripts in `GitHub/_*.py` (audit / dump / apply / assign / verify / fix-gaps). A2 in [`DECISIONS_NEEDED.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/DECISIONS_NEEDED.md) marked done; handoff updated.

- **2026-05-30 Corrected a real macro-analysis error (caught by M.G.).** SKD/VCP are **citation-dense, not "citation-free"** — they cite via indigenous quotations `“…”` + abbreviated authorities (`jE0`/`BA0`/`amara0`) + `iti`, with no `<ls>` tag. Fixed `macro_profile.py` (detect both citation styles + stratified sampling across the alphabet, not first-N) → **VCP 95% cited, SKD 51%**. Fixed `micro_entry.py` (Patel headword-convention variants + homonym concatenation) → `dharma` now resolves across 10 dicts (**VCP 17,131 chars/41 cites, SKD 8,774/24**). Roadmap finding corrected. csl-observatory `fdb9e6f`. **Lesson: validate heuristic detectors against real entries; use mid-alphabet test lemmas.**

- **2026-05-30 Research + practitioner layer (roadmap + 2 prototypes) + decisions doc.** Added [`RESEARCH_LAYER_ROADMAP.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/RESEARCH_LAYER_ROADMAP.md) (csl-observatory `5480bf8`) — additive to the genealogy/typology program: 7 new testable hypotheses, micro+macro viz specs, practitioner tools for students/makers/researchers. Two runnable prototypes from real csl-orig data: `scripts/lexico/micro_entry.py` (one-lemma×dicts feature matrix — `gam`: PWG 100,962 chars/1,299 cites vs MW 7,132/115; AP `gam` a 69-char stub to investigate) and `scripts/lexico/macro_profile.py` (43-dict structural-profile heatmap — citation-dense PWG/SCH/BHS vs citation-free SKD/VCP/CCS). Also added [`DECISIONS_NEEDED.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/DECISIONS_NEEDED.md) (`8931cf4`) — the living maintainer-decision queue (memory `feedback_pending_decisions` resurfaces it). Both linked from the observatory README.

- **2026-05-30 Further autonomous roadmap work (no human needed).**
  - Extended the csl-orig encoding guard with a `<L>/<LEND>` structural-balance check — csl-orig `cbcf6be`; verified clean on all 43 canonical sources. Covers most of csl-pywork#51 (only the full `make_xml` parse remains).
  - Added `csl-observatory/scripts/clone_org.py` — csl-observatory `97b6267`; idempotent, non-destructive org-sibling cloner (Roadmap M5).
  - Read-only taxonomy audit: 22/77 repos have open issues but no taxonomy milestones (COLOGNE 212, CORRECTIONS 90, VCP/csl-apidev 22, …) — recorded as the scope for a supervised `/cologne-runbook-all`.

- **2026-05-30 Roadmap quick wins Q1–Q6 implemented (autonomous).**
  - **Q1** `hw.py` now reads inputs with `utf-8-sig` (init_entries / init_entries_kosha / init_hwextra) — csl-pywork `e6d0f30`, **closes #50**.
  - **Q3** `.gitattributes` (`* text=auto` + binary patterns) added to csl-observatory + the 6 dict repos (csl-pywork/csl-orig/csl-corrections already had one).
  - **Q2** encoding-guard CI in csl-orig `3444aa3` — `.github/workflows/encoding-guard.yml` + `scripts/check_encoding.py` (BOM + valid-UTF-8 on the 43 canonical `v02/<dict>/<dict>.txt`; NFC omitted to avoid false-positives; XML-parse left on #51).
  - **Q4/Q5/Q6** csl-observatory `e0eba83` (**closes #9/#10/#11**): `data/contributors_map.json` wired into `contributor_stats.py`; `scripts/validate_mermaid.py`; `docs/AI_CONTRIBUTION_POLICY.md`.
  - Commit-trailer convention applied throughout (dict repos: none; infra repos: `Co-Authored-By`).

- **2026-05-30 Ecosystem improvement & optimization roadmap + tracking issues.** [`csl-observatory/docs/ROADMAP.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/ROADMAP.md) (commit `efcb0e9`, linked from README) — prioritized quick-wins → medium → strategic across reliability, data-quality, automation, docs/governance, grounded in this cycle's evidence. Opened 7 issues on tooling repos: csl-pywork [#51](https://github.com/sanskrit-lexicon/csl-pywork/issues/51) (CI BOM/NFC/XML guard), [#52](https://github.com/sanskrit-lexicon/csl-pywork/issues/52) (`.gitattributes`), [#53](https://github.com/sanskrit-lexicon/csl-pywork/issues/53) (modernise refresh script); csl-observatory [#9](https://github.com/sanskrit-lexicon/csl-observatory/issues/9) (identity map), [#10](https://github.com/sanskrit-lexicon/csl-observatory/issues/10) (Mermaid CI), [#11](https://github.com/sanskrit-lexicon/csl-observatory/issues/11) (bot/AI commit+comment policy), [#12](https://github.com/sanskrit-lexicon/csl-observatory/issues/12) (automate refresh). (Q1 hw.py = csl-pywork#50, filed earlier.)

- **2026-05-30 Contributor stats expanded to org-wide line churn.** `CONTRIBUTOR_STATS.md` churn now spans 70 repos (7 binary/generated-data repos excluded) via git; generator gained `--full-churn` (temp-clones repos lacking a full local clone). README links the report. Commit `33c0179`. Consolidated identities: funderburkjim 4,596 / drdhaval2785 3,467 / gasyoun 1,031 commits.

- **2026-05-30 Org-wide contributor & work statistics → [`csl-observatory/docs/CONTRIBUTOR_STATS.md`](https://github.com/sanskrit-lexicon/csl-observatory/blob/main/docs/CONTRIBUTOR_STATS.md)** (commit `175241e`). Covers all 77 non-fork org repos: commits/tenure/time-series (commits API), year×month commit grid, per-contributor & per-repo breakdowns, issues & PRs by opener, line-churn for the locally-cloned dict repos. Headlines: 9,424 commits, 23 authors, 5,235 issues, span 2014-01→2026-05; funderburkjim 4,496 / drdhaval2785 3,465 / gasyoun 1,029 commits. Reproducible via `csl-observatory/scripts/contributor_stats.py`. **Note:** `/stats/contributors` & `/stats/code_frequency` stayed HTTP 202 across the burst, so org-wide line-churn was unavailable via API → used git for local clones only; full org churn needs all repos cloned.

- **2026-05-30 Full runbook docs (phases 8–16) — BUR, BOP, INM, KRM, MW72, BOR:** Expanded each repo's `README.md` (contents, timeline, live Projects & Milestones + Mermaid charts reflecting the fresh taxonomy, issue typology, source bibliography, encoding declaration, pipeline DAG) and `CLAUDE.md` (architecture, commands, data-format + annotated example — appended to the 4 already-detailed CLAUDE.md files; full-written for the KRM/BUR stubs). Added `LICENSE` (CC BY-SA 4.0 pointer), `CHANGELOG.md`, and `SECURITY/CONTRIBUTING/CODE_OF_CONDUCT.md` where missing (preserved BUR/KRM's own); created MW72 `CITATION.cff`. All 18 Mermaid blocks validated via `gh api markdown`. Committed + pushed per repo with plain `docs:` messages (**no Claude trailer**, per dict-repo convention). Working trees were first un-shallowed (the 5 were `.git`-only shells) and BOR freshly cloned. Commits: BUR `885dec1`, BOP `64ad093`, INM `53b0b42`, KRM `aca56e7`, MW72 `441046c`, BOR `5c6e6cb`.

- **2026-05-30 hw.py BOM-hardening:** Filed [csl-pywork#50](https://github.com/sanskrit-lexicon/csl-pywork/issues/50) — `bug`/`minor`/Developer Experience. Proposes `utf-8-sig` in `init_entries`/`init_entries_kosha` to tolerate a leading BOM instead of crashing with the cryptic "init_entries Error 2" message.

- **2026-05-30 Taxonomy core — BOR, BUR, INM, KRM, BOP, MW72 (phases 1–7):** Created 4 milestones per repo (DTB/DQ/SD/ME as 1–4); created/recolored all type (`#0075ca`) and severity labels; classified all 31 issues across 6 repos (exactly one type + one severity + matching milestone + org project); removed stale default labels (`bug`/`question`/`enhancement`/`documentation` where they conflicted with the taxonomy type); verified with Phase 7 script — ALL OK on all 6. BOR: 4 issues; BUR: 6; INM: 11; KRM: 4; BOP: 8; MW72: 6. No local file changes (GitHub-API-side only, no commits needed).

- **2026-05-29 Task 1** — Appended the `batch_20260529` audit-trail link in-place to the 10 already-closed markup-issue comments (BOR#4, AP90#30, GRA#37, MWS#194, AP#29, BUR#6, INM#10, KRM#4, BOP#8, MW72#6); set the "Structured Data" milestone on AP90/GRA/MWS/AP. (Issues were already closed + labeled `markup`+`minor` by a prior session.)
- **2026-05-29 Task 2** — Diagnosed the `<LEND>` error as a resolved BOM artifact (`922602c`); proved via git archaeology; no source change; corrected the memory note.
- **2026-05-29 Task 3** — Go/no-go = **NO-GO local**; refresh is server-side cron. Verified all 8 csl-orig fix commits are on `origin/master` so the server will propagate.
- **2026-05-29 Task 4** — Mirrored the Phase 9 "Documentation callout (preserve-if-present)" edit from the local skill copies into the canonical `csl-observatory` runbooks (`cologne-tooling-runbook.md` + `cologne-issue-runbook.md`); commit `552def7`, pushed to `main`.
